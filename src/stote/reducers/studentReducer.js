import data from "../../Data/listStudent.json"

const DEFAULT_STATE = {
    listStudent : data,
};


export const studentReducer = (state = DEFAULT_STATE, action) => {
   switch (action.type) {
    // case value:
        
    //     break;
   
    
   };


   return {...state}
};